import { createStyles } from "../../styles/create-styles";

export const useStyles = () =>
  createStyles({
    forms: {
      display: "flex",
      justifyContent: "space-between",
      marginLeft: 100,
      marginRight: 100,
    },
  });
