import { createStyles } from "../../styles/create-styles";

export const useStyles = () =>
  createStyles({
    classes: {
      marginTop: '5vh',
      marginLeft: '6vw',
      display: "flex",
      width: '95%',
      flexWrap: "wrap",
    },
  });
