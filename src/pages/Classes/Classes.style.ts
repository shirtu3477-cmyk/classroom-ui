import { createStyles } from "../../styles/create-styles";

export const useStyles = () =>
  createStyles({
    classes: {
      marginTop: 150,
      marginLeft: 100,
      display: "flex",
      width: 1800,
      height: 400,
      flexWrap: "wrap",
    },
  });
