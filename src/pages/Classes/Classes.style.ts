import { createStyles } from "../../styles/create-styles";

export const useStyles = () =>
  createStyles({
    classes: {
      marginTop: '3vh',
      marginLeft: '1vw',
      display: "flex",
      flexWrap: "wrap",
    },
  });
